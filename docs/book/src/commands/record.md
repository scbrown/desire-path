# dp record (deprecated)

`record` is a compatibility entrypoint. New integrations should use
[`dp ingest --source <name>`](ingest.md), which records invocations and also
stores failed calls as desires. See the [first-success fixture](../getting-started.md)
for a complete example.
